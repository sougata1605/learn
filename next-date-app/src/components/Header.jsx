import React from "react";

export default function Header() {
  return (
    <header className="header">
      <h1> Date Calculator on user input </h1>
      <p>Enter a number of days — app shows the date after/before  that many days.</p>
    </header>
  );
}