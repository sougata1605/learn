import React from "react";

export default function Header() {
  return (
    <header className="header">
      <h1>Next Date Calculator</h1>
      <p>Enter a number of days — app shows the date after that many days.</p>
    </header>
  );
}